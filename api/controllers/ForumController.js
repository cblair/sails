/**
 * ForumController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `ForumController.title()`
   */
  title: async function (req, res) {
    return res.json({
      todo: 'title() is not implemented yet!'
    });
  },

  /**
   * `ForumController.body()`
   */
  body: async function (req, res) {
    return res.json({
      todo: 'body() is not implemented yet!'
    });
  }

};

